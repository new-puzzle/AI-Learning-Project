# Core dependencies for Learning Path Generator
anthropic>=0.18.0

# Optional: Only needed if using input_form.py (Streamlit frontend)
streamlit>=1.28.0

# Optional: Only needed if using api_server.py (React/TypeScript frontend)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

